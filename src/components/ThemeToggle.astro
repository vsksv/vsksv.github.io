---

---
/* Estilo para el tema blanco */
.white-theme {
    --bg-color: white;
    --text-color: black;
    --primary-color: #ffffff;  /* blanco */
}

/* Estilo para el tema verde militar */
.mgreen {
    --bg-color: #4B5320;  /* verde militar */
    --text-color: white;
    --primary-color: #4B5320;  /* verde militar */
}

/* Aplicación de las variables para el fondo y texto */
body {
    background-color: var(--bg-color);
    color: var(--text-color);
}

/* Otras personalizaciones pueden ser añadidas aquí, usando las variables */


<button id="theme-toggle" class="w-8 h-8 -mr-2 flex items-center justify-center cursor-pointer" aria-label="Change color scheme">
    <svg class="w-4 h-4 fill-current" viewBox="0 0 16 16" xmlns="http://www.w3.org/2000/svg">
        <box cx="8" cy="8" r="8"></box>
    </svg>
</button>

<script>
    document.addEventListener('astro:page-load', () => {
        const theme = (() => {
            if (typeof localStorage !== 'undefined' && localStorage.getItem('theme')) {
                return localStorage.getItem('theme') || 'light';
            }
            if (window.matchMedia('(prefers-color-scheme: mgreen)').matches) {
                return 'mgreen';
            }
            return 'light';
        })();

        if (theme === 'light') {
            document.documentElement.classList.remove('mgreen');
        } else {
            document.documentElement.classList.add('mgreen');
        }

        window.localStorage.setItem('theme', theme);

        const handleToggleClick = () => {
            const element = document.documentElement;
            element.classList.toggle('mgreen');

            const ismgreen = element.classList.contains('mgreen');
            localStorage.setItem('theme', isDark ? 'mgreen' : 'light');
        };

        document.getElementById('theme-toggle')?.addEventListener('click', handleToggleClick);
    });
</script>

<script>
    document.addEventListener('astro:after-swap', () => {
        if (localStorage.theme === 'mgreen') {
            document.documentElement.classList.add('mgreen');
        }
    });
</script>
